<% my $numForests = scalar @{$forests}; %>
{
    "numForests": "<%== $numForests %>",
    "forests": [
        <% for my $i (0..($numForests - 1)) { %>
        {
            "id": "<%== $forests->[$i]->{'id'} %>",
            "name": "<%== $forests->[$i]->{'name'} %>",
            "description": "<%== $forests->[$i]->{'description'} %>"
        }
        <%== "," if $i != ($numForests - 1) %>
        <% } %>
    ]
}
